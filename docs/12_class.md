

no inheritance, consider to use components patterns
no interface, consider to use enum with member data instead


class constructor, if param has val / var modifier, it is the member of the class, otherwise it is local variable / param only

class class_name(
    val x : i32, 
    val y : i32)

    val another_member : i32 = 123

    fun member_function
        do something



multi classed can be included in the same file


