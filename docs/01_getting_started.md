
# TODO

micro panda is an embed programming language.

let's add this part once we finished the implementation, as it include installing, setting up project, etc


no heap, everything is static.
we can create object from allocator, witch based on static array. allocator can be reset completely (no fragment)
